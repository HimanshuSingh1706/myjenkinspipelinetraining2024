pipeline

agent any

stage('SCM') {
	steps {
		echo "git pull my code step 1"
		echo "git pull my code step 1"
	}
}

stage('Deploy') {
	steps {
		echo "deploying my code"
	}
}

stage('test')
	step {
		echo "test my final webapp"
	}
}